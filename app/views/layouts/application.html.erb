<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Zero-Waste Mysuru" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Zero-Waste Mysuru" %>">
    <%= favicon_link_tag image_path("favicon.ico") %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true, async: Rails.env.production? %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <%= render 'layouts/messages' %>
    <%= yield %>
    <%= render 'layouts/footer' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <!-- Custom Fonts -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">
  </body>
</html>
